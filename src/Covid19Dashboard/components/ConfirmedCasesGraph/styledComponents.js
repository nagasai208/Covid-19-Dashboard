import styled from '@emotion/styled';
import tw from 'tailwind.macro';

const ConfirmedCasesDiv = styled.div`
${tw`text-white bg-gray-800 p-3`}`;

const GraphHeading = styled.p`
${tw`ml-5`}`

export {
    ConfirmedCasesDiv, GraphHeading
}
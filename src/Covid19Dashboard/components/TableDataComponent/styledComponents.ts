import styled from '@emotion/styled';
import tw from 'tailwind.macro';

const TableMainDiv = styled.div`
${tw`text-white text-center bg-gray-800 shadow-2xl  `}
width:100%`

export {
    TableMainDiv
}